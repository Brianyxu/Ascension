import React, { Component } from 'react';
import { Row, Container, Col } from 'react-bootstrap';

class Schedule extends Component {
    render() {
        return (
            <div className="outer">
                <Container className="inner">
                    Not sure how to do schedule right now

                </Container></div>
        );
    }
}

export default Schedule;